# Skills

Portable, lightweight `SKILL.md` templates injected dynamically into worker
system prompts (Phase 8). Each skill lives in `skills/<name>/SKILL.md` with
YAML frontmatter (`name`, `description`, when-to-use guidance) followed by
Markdown instructions.

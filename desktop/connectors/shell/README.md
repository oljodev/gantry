# Shell

Run commands in an attached folder with streaming output, timeouts and kill.

First-party, native (Rust) connector. Design: `docs/plan/03-connector-system.md` §5. Tools are listed in `manifest.json` once implemented.

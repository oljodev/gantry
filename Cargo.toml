[workspace]
resolver = "3"
members = [
  "src-tauri",
  "crates/*",
  "connectors/filesystem",
  "connectors/code-editor",
  "connectors/shell",
  "connectors/web",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "FSL-1.1-ALv2"
repository = "https://github.com/oljodev/gantry"
homepage = "https://oljo.dev"
rust-version = "1.93"
publish = false

[workspace.dependencies]
# Gantry crates
gantry-core = { path = "crates/gantry-core" }
gantry-store = { path = "crates/gantry-store" }
gantry-secrets = { path = "crates/gantry-secrets" }
gantry-providers = { path = "crates/gantry-providers" }
gantry-connectors = { path = "crates/gantry-connectors" }
gantry-workspace = { path = "crates/gantry-workspace" }
gantry-agent = { path = "crates/gantry-agent" }
gantry-connector-filesystem = { path = "connectors/filesystem" }
gantry-connector-code-editor = { path = "connectors/code-editor" }
gantry-connector-shell = { path = "connectors/shell" }
gantry-connector-web = { path = "connectors/web" }

# Shared third-party versions. Pin here, inherit everywhere.
serde = { version = "1", features = ["derive"] }
serde_json = "1"
specta = { version = "=2.0.0-rc.25", features = ["derive", "ulid"] }
thiserror = "2"
anyhow = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "fs", "process"] }
tracing = "0.1"
ulid = { version = "1", features = ["serde"] }

[profile.release]
lto = "thin"
codegen-units = 1
strip = true

[profile.dev.package."*"]
opt-level = 1
